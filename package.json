{
  "dependencies": {
    "animate.css": "^4.1.1",
    "primereact": "^8.7.3"
  },
  "scripts": {
    "prestart": "cd ./client && npm install && cd .. && cd ./server && npm install && cd .. && cd ./socket && npm install"
  }
}
